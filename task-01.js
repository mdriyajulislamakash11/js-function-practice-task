/* Task-1
Take four parameters. Multiply the four 
numbers and then return the result */


function sum (a, b, c, d) {
     let multiplay = a* b* c* d;
      return multiplay;
};
const total = sum(2, 2, 2, 2);
console.log(total)